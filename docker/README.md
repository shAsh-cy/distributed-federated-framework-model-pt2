# Docker setup
This folder contains Dockerfiles for server and client and a docker-compose file to run multiple clients.

## Steps
1. Build images:
   ```bash
   docker-compose build
   ```
2. Start services:
   ```bash
   docker-compose up
   ```
3. The server will be available at port 8080.
