/** Stub — the real History view lands in its own commit. */
export function HistoryView(_props: { onOpenRun: (runId: string) => void }) {
  return <p className="font-prose text-sm text-slate">History view arrives shortly.</p>;
}
