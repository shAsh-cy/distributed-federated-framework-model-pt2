/** Stub — the real Results view lands in its own commit. */
export function ResultsView(_props: Record<string, unknown>) {
  return <p className="font-prose text-sm text-slate">Results view arrives shortly.</p>;
}
