/** Stub — the real Configure view lands in its own commit. */
export function ConfigureView(_props: { onStarted: (runId: string) => void }) {
  return <p className="font-prose text-sm text-slate">Configure view arrives shortly.</p>;
}
