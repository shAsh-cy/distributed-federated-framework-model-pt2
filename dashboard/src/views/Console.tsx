/** Stub — the real Console view lands in its own commit. */
export function ConsoleView(_props: { runId: string | null }) {
  return <p className="font-prose text-sm text-slate">Console view arrives shortly.</p>;
}
