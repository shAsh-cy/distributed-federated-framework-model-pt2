/** Placeholder shell — replaced by the real shell in the next commit. */
import { Button } from "./ui/primitives";

export function App() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-6">
      <h1 className="font-head text-xl uppercase tracking-head">
        Federated Learning Coordinator
      </h1>
      <p className="readout mt-2 text-sm">instrument fascia · shell lands next commit</p>
      <Button className="mt-4">Primitive check</Button>
    </div>
  );
}
