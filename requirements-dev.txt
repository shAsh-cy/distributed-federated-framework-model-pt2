# Development / test dependencies. Every version pinned exactly.
# Install on top of requirements.txt:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
coverage==7.15.2
ruff==0.16.0
